def get_character_record(name, server, level, rank):
    return {
        "name": name,
        "server": server,
        "level": level,
        "level": level,
        "rank": rank,
        "rank": rank,
    }
